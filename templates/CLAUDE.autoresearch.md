# DiT AutoResearch

Read `SKILL.md` and `program.md` before editing code. The fixed evaluator is ground truth. Do not modify `fixed_paths`. Target hardware is RTX A5500/Ampere. Use the local handbook through `dit-ar search`. Quality cannot regress; quantization requires strict benchmark parity. Run baseline before experiments and use one hypothesis per commit.
